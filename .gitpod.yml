# This configuration file was automatically generated by Gitpod.
# Please adjust to your needs (see https://www.gitpod.io/docs/introduction/learn-gitpod/gitpod-yaml)
# and commit this file to your remote git repository to share the goodness with others.

# Learn more from ready-to-use templates: https://www.gitpod.io/docs/introduction/getting-started/quickstart

tasks:
  - init: wget -O Miniforge3.sh "https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-$(uname)-$(uname -m).sh" && bash Miniforge3.sh -b -u -p "${HOME}/conda" && source "${HOME}/conda/etc/profile.d/conda.sh" && conda env update -n base -f environment.yml && conda install --yes -c https://repo.anaconda.com/pkgs/snowflake snowflake-snowpark-python openai && pip install -r requirements.txt && rm Miniforge3.sh && streamlit run streamlit_app.py
